import button from './../components/button';

var buttonAdapter = new Object();
buttonAdapter.render = function(inputObject) {
  var view = buttonAdapter.render(inputObject);
  return view;
}

export default buttonAdapter;